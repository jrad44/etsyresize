{
  "name": "resizedimage",
  "version": "1.0.0",
  "description": "Simple image resizer for marketplace presets with Pro unlock via Stripe",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "1.4.5-lts.1",
    "sharp": "^0.34.3",
    "archiver": "^5.3.1",
    "express-rate-limit": "^6.7.0"
  }
}
